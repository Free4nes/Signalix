# Copy to .env and replace values. Environment variables override .env.
# DATABASE_URL: replace YOUR_PASSWORD with your PostgreSQL password.
# If password contains @ : / # % etc., use percent-encoding (e.g. @ = %40).
DATABASE_URL=postgres://postgres:YOUR_PASSWORD@localhost:5432/messenger?sslmode=disable
JWT_SECRET=your-secret-key-at-least-32-characters-long
OTP_SALT=your-otp-salt
OTP_DEV_MODE=true
PORT=8080
